<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="jp.myapp.dao.mapper.UsersMapper">

    <resultMap type="jp.myapp.dao.entity.UsersImpl" id="users">
        <id column="USER_ID" property="userId" />
        <result column="USERNAME" property="username" />
        <result column="PASSWORD" property="password" />
        <result column="PASSWORD_UPDATED_DATETIME" property="passwordUpdatedDatetime" />
        <result column="ENABLED" property="enabled" />
        <result column="PROVISIONAL_REGISTRATION" property="provisionalRegistration" />
        <result column="LAST_LOGIN" property="lastLogin" />
        <result column="REGISTER_DATETIME" property="registerDatetime" />
        <result column="REGISTER_USER_ID" property="registerUserId" />
        <result column="UPDATED_DATETIME" property="updatedDatetime" />
        <result column="UPDATED_USER_ID" property="updatedUserId" />
        <result column="VERSION" property="version" />
    </resultMap>

    <select id="select" parameterType="string" resultMap="users">
        <![CDATA[
            SELECT * FROM USERS WHERE USER_ID = #{userId}
        ]]>
    </select>

    <select id="selectForUpdate" parameterType="string" resultMap="users">
        <![CDATA[
            SELECT * FROM USERS WHERE USER_ID = #{userId} FOR UPDATE
        ]]>
    </select>

    <insert id="insert" parameterType="jp.myapp.dao.entity.Users">
        <![CDATA[
            INSERT INTO USERS
            ( USER_ID, USERNAME, PASSWORD, PASSWORD_UPDATED_DATETIME, ENABLED, PROVISIONAL_REGISTRATION, LAST_LOGIN, REGISTER_DATETIME, REGISTER_USER_ID, UPDATED_DATETIME, UPDATED_USER_ID, VERSION )
            VALUES
            ( #{userId}, #{username}, #{password}, #{passwordUpdatedDatetime,jdbcType=TIMESTAMP}, #{enabled}, #{provisionalRegistration}, #{lastLogin,jdbcType=TIMESTAMP}, #{registerDatetime,jdbcType=TIMESTAMP}, #{registerUserId}, #{updatedDatetime,jdbcType=TIMESTAMP}, #{updatedUserId}, #{version} )
        ]]>
    </insert>

    <update id="update" parameterType="jp.myapp.dao.entity.Users">
        <![CDATA[
            UPDATE USERS
            SET USERNAME = #{username},
                PASSWORD = #{password},
                PASSWORD_UPDATED_DATETIME = #{passwordUpdatedDatetime,jdbcType=TIMESTAMP},
                ENABLED = #{enabled},
                PROVISIONAL_REGISTRATION = #{provisionalRegistration},
                LAST_LOGIN = #{lastLogin,jdbcType=TIMESTAMP},
                REGISTER_DATETIME = #{registerDatetime,jdbcType=TIMESTAMP},
                REGISTER_USER_ID = #{registerUserId},
                UPDATED_DATETIME = #{updatedDatetime,jdbcType=TIMESTAMP},
                UPDATED_USER_ID = #{updatedUserId},
                VERSION = #{version}
            WHERE USER_ID = #{userId}
        ]]>
    </update>

    <delete id="delete" parameterType="string">
        <![CDATA[
            DELETE FROM USERS WHERE USER_ID = #{userId}
        ]]>
    </delete>

    <select id="selectAll" resultMap="users">
        <![CDATA[
            SELECT * FROM USERS
        ]]>
    </select>

</mapper>
